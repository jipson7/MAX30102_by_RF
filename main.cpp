
#include <iostream>
#include <string>

#include "algorithm_by_RF.h"
#include "max30102.h"
#include "algorithm.h"

int main() {
  for (std::string line; std::getline(std::cin, line);) {
    std::cout << line << std::endl;
  }
  return 0;
 }
